#!/usr/bin/env bash
# The command to cut an entire line
# Write the answer into the file /root/0x01_emacs/cutting.

mkdir -p 0x01_emacs
echo "C-k" > /root/0x01_emacs/cutting
ls -l /root/0x01_emacs/cutting

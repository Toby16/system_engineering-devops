#!/usr/bin/env bash
# The command to save a file?
# Write the answer into the file /root/0x01_emacs/saving.

mkdir -p 0x01_emacs
echo "C-x C-s" > /root/0x01_emacs/saving
ls -l /root/0x01_emacs/saving
